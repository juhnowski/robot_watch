<?xml version="1.0" encoding="utf-8"?>
<android.support.wear.widget.BoxInsetLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:background="@color/white"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MessageActivity"
        tools:deviceIds="wear"
        android:padding="@dimen/box_inset_layout_padding">

    <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="@dimen/inner_frame_layout_padding"
            android:orientation="vertical">
        <TextView
                android:text="Кому..."
                android:layout_width="match_parent"
                android:layout_height="wrap_content" android:id="@+id/textUser"
                android:textAppearance="@android:style/TextAppearance.DeviceDefault.Large"
                android:textAlignment="center" android:textColor="@color/colorPrimaryDark" android:onClick="users"/>
        <EditText
                android:layout_width="match_parent"
                android:layout_height="97dp"
                android:inputType="textMultiLine"
                android:ems="10"
                android:id="@+id/editText4" android:textColor="@color/colorBlack"
                android:text="Отправляю два болта и гайку. Если не подойдет - присылайте обратно"
                />
        <ImageView
                android:src="@drawable/call"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" android:id="@+id/imageView3" android:onClick="process"/>
    </LinearLayout>
</android.support.wear.widget.BoxInsetLayout>