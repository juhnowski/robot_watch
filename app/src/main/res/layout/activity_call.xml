<?xml version="1.0" encoding="utf-8"?>
<android.support.wear.widget.BoxInsetLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:background="@color/white"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".CallActivity"
        tools:deviceIds="wear"
        android:padding="@dimen/box_inset_layout_padding">

    <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="@dimen/inner_frame_layout_padding"
            android:orientation="vertical">
        <TextView
                android:text="Вызвать"
                android:layout_width="match_parent"
                android:layout_height="60sp" android:id="@+id/textView"
                android:textAppearance="@android:style/TextAppearance.DeviceDefault.Large"
                android:textAlignment="center" android:textColor="@color/colorPrimaryDark"/>
        <ImageView
                android:src="@drawable/call"
                android:layout_width="match_parent"
                android:layout_height="74sp" android:id="@+id/imageView2" android:onClick="run"/>
    </LinearLayout>
</android.support.wear.widget.BoxInsetLayout>